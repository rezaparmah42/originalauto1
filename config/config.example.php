<?php
// Example configuration for Original Shargh.
// Copy to config/config.php or populate the same environment variables on the
// server. NEVER commit real production secrets.

// Site name
// define('SITE_NAME', 'اورجینال شرق');

// Application environment and URL
// APP_ENV=production
// APP_URL=https://originalauto.ir

// APP_KEY must be set from the environment when deployed.
// APP_KEY=replace-with-production-secret-key

// Database credentials must be provided via environment variables.
// DB_HOST=localhost
// DB_NAME=your_database_name
// DB_USER=your_database_user
// DB_PASS=your_database_password

// Example .env-style values for local and production:
// APP_ENV=development
// APP_URL=http://localhost/originalauto
// APP_KEY=replace-this-with-a-secure-random-string
// DB_HOST=localhost
// DB_NAME=original_east
// DB_USER=root
// DB_PASS=

// Production defaults are intentionally strict:
// - APP_ENV defaults to production
// - SITE_URL defaults to https://originalauto.ir
// - DB_* values are required from environment variables in production
// - Missing production DB values should log an error and stop safely

// The runtime config file is config/config.php and is excluded from Git.
