<?php
// Example configuration for Original Shargh
// Copy to config/config.php or use environment variables to configure the
// runtime. DO NOT commit real secrets into this file.

// Application environment: one of 'production' or 'development'
// Set via environment variable: APP_ENV=production
// APP_ENV=development

// Application URL (used for canonical/OG/sitemap generation)
// Example local development value:
// APP_URL=http://localhost/originalshargh
// Example production value:
// APP_URL=https://originalauto.ir

// Application key used for HMACs and other secrets. Do NOT commit real
// production APP_KEY into source control. Provide via environment instead.
// APP_KEY=replace-with-production-secret-key

// Database (provide via environment variables on the server)
// DB_HOST=localhost
// DB_NAME=your_database_name
// DB_USER=your_database_user
// DB_PASS=your_database_password

// Example .env-style content (for reference only):
// APP_ENV=development
// APP_DEBUG=true
// APP_URL=http://localhost/originalshargh
// APP_KEY=replace-this-with-a-secure-random-string
// DB_HOST=localhost
// DB_NAME=original_db
// DB_USER=root
// DB_PASS=

// This file is documentation-only. The runtime configuration file is
// `config/config.php` which prefers environment variables (getenv) and
// applies safe fallbacks when necessary.
